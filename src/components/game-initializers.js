/**
 * Inicializa el juego Tres en Raya
 */
export function initTresEnRaya() {
    if (typeof initializeTresEnRaya === 'function') {
        initializeTresEnRaya();
    }
}

/**
 * Reinicia el juego Tres en Raya
 */
export function resetTresEnRaya() {
    if (typeof resetTresEnRayaGame === 'function') {
        resetTresEnRayaGame();
    }
}

/**
 * Inicializa el juego Puzzle de Memoria
 */
export function initPuzzle() {
    if (typeof initializePuzzle === 'function') {
        initializePuzzle();
    }
}

/**
 * Reinicia el juego Puzzle de Memoria
 */
export function resetPuzzle() {
    if (typeof resetPuzzleGame === 'function') {
        resetPuzzleGame();
    }
}

/**
 * Inicializa el juego Piedra, Papel o Tijera
 */
export function initPiedraPapelTijera() {
    if (typeof initializePiedraPapelTijera === 'function') {
        initializePiedraPapelTijera();
    }
} 